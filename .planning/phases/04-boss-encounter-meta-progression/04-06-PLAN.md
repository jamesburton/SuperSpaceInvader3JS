---
phase: 04-boss-encounter-meta-progression
plan: "06"
type: execute
wave: 4
depends_on: ["04-05"]
files_modified: []
autonomous: false
requirements:
  - BOSS-01
  - BOSS-02
  - BOSS-03
  - BOSS-04
  - META-01
  - META-02
  - META-03
  - META-04
  - META-05
  - META-06
  - META-07

must_haves:
  truths:
    - "Boss encounter triggers after wave 10 and the boss is visually 3-4x larger than normal enemies"
    - "Boss health bar is visible throughout the fight with segmented phase boundary and current phase label"
    - "Boss transitions from Phase 1 (red, aimed spread) to Phase 2 (orange, sweeping beam) at 50% HP"
    - "Meta shop opens from title screen via U key and shows SI$ balance with all upgrades"
    - "Purchasing a meta upgrade deducts SI$ and persists after browser refresh"
    - "Run-end screen shows SI$ earned this run alongside total meta balance"
    - "Meta upgrades apply at run start (speed/fire rate bonuses visible in gameplay)"
  artifacts:
    - path: "src/entities/Boss.ts"
      provides: "BossEnemy mesh visible in-game, significantly larger than enemies"
      exports: ["BossEnemy"]
    - path: "src/ui/BossHealthBar.ts"
      provides: "Segmented health bar visible during boss fight"
      exports: ["BossHealthBar"]
    - path: "src/ui/MetaShopUI.ts"
      provides: "Meta shop overlay accessible from title screen"
      exports: ["MetaShopUI"]
    - path: "src/state/MetaState.ts"
      provides: "Zustand store persisting SI$ and purchasedUpgrades to localStorage key ssi-meta-v1"
      exports: ["useMetaStore"]
  key_links:
    - from: "src/systems/SpawnSystem.ts"
      to: "src/states/PlayingState.ts"
      via: "bossPending flag triggers boss.activate()"
      pattern: "bossPending"
    - from: "src/state/MetaState.ts"
      to: "localStorage"
      via: "Zustand persist with key ssi-meta-v1, version 1"
      pattern: "ssi-meta-v1"
---

<objective>
Human verification checkpoint for all 11 Phase 4 requirements. Verify the boss encounter, meta shop, meta progression, and run-end screen function correctly end-to-end.

Purpose: All previous plans are autonomous implementation — this checkpoint confirms everything works as a system from a player's perspective before the phase is marked complete.

Output: Human sign-off on all Phase 4 success criteria.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: Full Phase 4 verification — boss encounter + meta progression</name>
  <action>Human plays the game and verifies all 11 Phase 4 requirements below. No code changes are expected unless issues are found. Run `npm run dev` and open http://localhost:5173</action>
  <what-built>
Phase 4 complete implementation:
- Two-currency system: Gold (in-run booster shop) + SI$ (persistent meta currency)
- Chapter 1 boss: 80×60 unit neon red ship, triggers after wave 10, 2-phase attack machine
- Boss health bar: segmented DOM overlay with phase boundary at 50% and phase label
- Phase 1→2 transition: 0.4s white flash then orange color shift, large camera shake, sweeping beam replaces aimed spread
- Meta shop: accessible via U key on title screen, shows all upgrades with SI$ costs
- Meta progression: 1 SI$ earned per wave cleared, 50 SI$ on boss defeat, persists across sessions
- Run-end screen: shows SI$ earned this run + total accumulated balance
- Purchased meta upgrades apply at run start (fire rate, speed, starting life, starting loadout power-ups)
  </what-built>
  <how-to-verify>
Run `npm run dev` and open the game at http://localhost:5173

**Test 1: Meta shop (META-02, META-03, META-04, META-05)**
1. Press U on the title screen — meta shop overlay appears with "-- META SHOP --" header
2. Confirm: SI$ balance shown at top (starts at 0 for fresh save)
3. Press ESC — shop closes, title screen returns
4. Open browser DevTools → Application → LocalStorage → find key `ssi-meta-v1`
5. Confirm the key exists with `metaCurrency` and `purchasedUpgrades` fields
6. Refresh the page — SI$ balance persists (META-05)

**Test 2: In-game currency and run-end screen (META-01, META-06)**
1. Start a game (press Space)
2. Clear wave 1 — run-end screen should show "SI$ EARNED: 1" when dying
3. Verify HUD shows "Gold: N" (not "SI$") for the booster shop currency
4. After clearing waves 5/10 (or dying), run-end screen shows total SI$ earned alongside total balance

**Test 3: Boss encounter (BOSS-01, BOSS-02, BOSS-03, BOSS-04)**
Note: Boss triggers after wave 10. To test faster, use browser console to force:
`runState.wave` manipulation is not exposed — play through waves or
reduce BOSS_TRIGGER_WAVE in browser dev mode via DevTools source editing.

Alternatively: Confirm `BOSS_TRIGGER_WAVE = 10` in constants.ts and trust the implementation.
Play to wave 10 if time permits, OR:
1. Open DevTools console on the game page
2. Confirm no JS errors during normal gameplay
3. Start a game, play for 2-3 waves — confirm Gold drops from enemies and the booster shop opens after wave 5/10

**Test 4: Meta bonuses apply (META-03, META-04)**
1. Open meta shop (U key), earn some SI$ by playing a run
2. Return to title, open meta shop again, purchase a passive upgrade
3. Start a new run — the bonus should be applied (fire rate or speed noticeably different)

**Test 5: Persistence across browser sessions (META-05, META-07)**
1. Play a run, earn SI$, purchase a meta upgrade
2. Close the browser tab completely, reopen at http://localhost:5173
3. Press U — meta shop shows previously purchased upgrades as OWNED, SI$ balance preserved
4. Check localStorage key is `ssi-meta-v1` (not `ssix_v1`)
  </how-to-verify>
  <resume-signal>
Type "verified" if all tests pass, or describe which tests failed and what was observed.
  </resume-signal>
</task>

</tasks>

<verification>
All 11 Phase 4 requirements verified by human tester:
- BOSS-01: Boss is visually distinct and significantly larger than normal enemies
- BOSS-02: Boss has at least 2 distinct attack phases (aimed spread → sweeping beam)
- BOSS-03: Boss health bar shown throughout with segmented phase indicators
- BOSS-04: Phase transition has telegraphed visual + behavioral change
- META-01: SI$ earned at run end, persists via localStorage
- META-02: Main menu (title screen) has UPGRADES/meta shop access
- META-03: Meta shop offers 2+ starting weapon loadout unlocks
- META-04: Meta shop offers passive stat upgrades (fire rate, speed, life) with tier caps
- META-05: All meta purchases persist across browser sessions
- META-06: Run-end screen shows SI$ earned this run + total accumulated
- META-07: localStorage schema versioned (ssi-meta-v1, saveVersion=1, migration hook)
</verification>

<success_criteria>
Human tester confirms all 11 Phase 4 requirements are met and responds with "verified".
</success_criteria>

<output>
After completion, create `.planning/phases/04-boss-encounter-meta-progression/04-06-SUMMARY.md`
</output>
