---
phase: 10-meta-shop-expansion
plan: 02
type: execute
wave: 2
depends_on: ["10-01"]
files_modified:
  - src/config/metaUpgrades.ts
  - src/state/MetaState.ts
  - src/ui/MetaShopUI.ts
  - src/config/difficultyRules.ts
  - src/config/waveConfig.ts
  - src/config/campaign.ts
  - src/systems/AISystem.ts
  - src/config/boss.ts
  - src/systems/BossSystem.ts
  - src/states/TitleState.ts
  - src/states/PlayingState.ts
autonomous: true
requirements: [SHOP-04, SHOP-05, SHOP-06, SHOP-07]

must_haves:
  truths:
    - "Hard difficulty becomes a purchasable entitlement that unlocks from the title/run-start path, not inside the shop flow itself"
    - "Hard modifies wave speed, formation width where allowed, and Sniper timing through one shared transformation layer used by both endless and campaign content"
    - "Nightmare cannot be purchased or selected until a Hard-clear marker exists"
    - "Nightmare adds more aggressive formation-break timing and a boss encounter with one additional attack phase via generalized multi-phase boss logic"
    - "Normal behavior remains unchanged when no higher difficulty is selected"
  artifacts:
    - path: "src/config/metaUpgrades.ts"
      provides: "Hard and Nightmare entitlement definitions plus any Hard-clear marker if implemented as a pseudo-upgrade"
      exports: ["META_UPGRADES"]
    - path: "src/config/waveConfig.ts"
      provides: "Difficulty-aware wave transformation entry point for endless mode"
      exports: ["getWaveConfig", "WAVE_CONFIGS"]
    - path: "src/config/campaign.ts"
      provides: "Difficulty-aware transformation for authored and algorithmic campaign waves"
      exports: ["CAMPAIGN_CHAPTER_1", "getAlgorithmicWaves"]
    - path: "src/systems/AISystem.ts"
      provides: "Difficulty-fed aggression timing knobs instead of hardcoded Normal-only timings"
      exports: ["AISystem"]
    - path: "src/config/boss.ts"
      provides: "Boss phase configuration generalized beyond exactly two phases"
      exports: ["BOSS_DEF"]
    - path: "src/systems/BossSystem.ts"
      provides: "Generic N-phase transition and firing logic that can drive a Nightmare-only third phase"
      exports: ["BossSystem"]
  key_links:
    - from: "src/states/TitleState.ts"
      to: "src/state/MetaState.ts"
      via: "reads unlocked difficulty options and persists the selected difficulty"
      pattern: "difficulty|setDifficulty|purchasedUpgrades"
    - from: "src/config/campaign.ts"
      to: "src/config/waveConfig.ts"
      via: "reuses the same difficulty transform model for authored and generated waves"
      pattern: "difficulty|transform|WaveConfig"
    - from: "src/systems/BossSystem.ts"
      to: "src/config/boss.ts"
      via: "iterates config-driven phase thresholds and attacks rather than assuming exactly two phases"
      pattern: "phases|startFraction|currentPhase"
---

<objective>
Implement the difficulty ladder for Meta Shop Expansion: Hard and Nightmare unlocks, shared difficulty rules across gameplay, Nightmare gating on Hard completion, and an isolated boss-system generalization that supports the extra Nightmare phase without spilling UI concerns into combat code.

Purpose: deliver SHOP-04 through SHOP-07 while keeping the boss refactor isolated from the later shop-UI reorganization and centralizing all difficulty tuning in one maintainable rules path.
Output: new difficulty entitlements, a reusable difficulty transform layer for wave/campaign/AI tuning, Hard-clear gating for Nightmare, and boss data/system support for N-phase progression.
</objective>

<execution_context>
@C:/Users/james/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-meta-shop-expansion/10-CONTEXT.md
@.planning/phases/10-meta-shop-expansion/10-RESEARCH.md
@.planning/phases/10-meta-shop-expansion/10-VALIDATION.md
@.planning/phases/10-meta-shop-expansion/10-01-PLAN.md

<interfaces>
Current constraints confirmed by research and source inspection:

- `src/systems/BossSystem.ts` explicitly transitions only from phase 1 to phase 2 at `<= 0.5` HP and selects attack behavior from a `phase: 1 | 2` branch.
- `src/config/boss.ts` types `phases` as an exact two-item tuple.
- `src/config/waveConfig.ts` and `src/config/campaign.ts` currently return authored/base wave configs with no difficulty transform layer.
- `src/systems/AISystem.ts` hardcodes flanker, sniper, and swooper timing values that Nightmare must override through an input/config seam rather than inline conditionals.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add purchasable difficulty entitlements and Hard-clear gating</name>
  <files>src/config/metaUpgrades.ts, src/state/MetaState.ts, src/ui/MetaShopUI.ts, src/states/TitleState.ts, src/states/PlayingState.ts</files>
  <action>
Extend `src/config/metaUpgrades.ts` with:
- one Hard difficulty unlock upgrade
- one Nightmare difficulty unlock upgrade
- if using the preferred research approach, one zero-cost pseudo-entitlement or equivalent marker representing "Hard campaign cleared"

Update the purchase and selection gating path so:
- Hard is purchasable and selectable only after its entitlement is owned
- Nightmare purchase is locked until the Hard-clear marker exists
- Nightmare selection is impossible until its purchase succeeds
- keep any `MetaShopUI` changes in this plan limited to purchase visibility/lock-state behavior needed for the new entitlements, leaving the tabbed layout rewrite to Plan 10-03

Record the Hard-clear marker on campaign victory at the narrowest correct seam, using the existing persistent channel instead of inventing a second unlock system unless there is a compelling technical reason not to.
  </action>
  <verify>
    <automated>npx vitest run src/state/MetaState.test.ts src/states/TitleState.test.ts src/states/PlayingState.test.ts src/ui/MetaShopUI.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Difficulty entitlements exist, Hard victory persists the Nightmare prerequisite, and TitleState only exposes selections the player has actually earned.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce one shared difficulty-rules path for waves and AI aggression</name>
  <files>src/config/difficultyRules.ts, src/config/waveConfig.ts, src/config/campaign.ts, src/systems/AISystem.ts, src/states/PlayingState.ts</files>
  <action>
Add a dedicated difficulty transformation seam rather than scattering `if (difficulty === ...)` across gameplay code. This can live in a new helper module if needed; if so, add it and include it in plan summary output even if not listed in frontmatter.

The shared rules path must:
- leave `normal` output behavior identical to current authored data
- apply Hard rules to both endless and campaign wave generation:
  - noticeable speed increase
  - `+1` enemy per wave where formation/layout caps allow it
  - Snipers becoming available one wave earlier than the base schedule
- apply Nightmare rules on top of or alongside Hard:
  - more aggressive flanker timing
  - earlier or denser swooper-group triggers
  - optional modest sniper cadence increase if needed for readability-balanced tuning
- avoid mutating the authored `WAVE_CONFIGS` source objects

Thread the selected difficulty from MetaState/run setup into the wave-generation and AI-update paths so the chosen rules are active before the first wave starts.
  </action>
  <verify>
    <automated>npx vitest run src/config/difficultyRules.test.ts src/config/waveConfig.test.ts src/states/PlayingState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Hard and Nightmare use one central difficulty transform for campaign and endless content, Normal remains unchanged, and AI aggression responds through explicit difficulty-fed inputs.</done>
</task>

<task type="auto">
  <name>Task 3: Generalize boss logic to multi-phase data and add Nightmare's extra phase</name>
  <files>src/config/boss.ts, src/systems/BossSystem.ts, src/states/PlayingState.ts</files>
  <action>
Refactor the boss system in two deliberate steps:

1. Generalize structure:
- change boss config typing from an exact two-phase tuple to an array model that supports N ordered phases
- update transition logic so phase changes are driven by configured thresholds instead of a single hardcoded `<= 0.5` check
- update movement/attack lookup so `BossSystem` selects behavior from the current phase index generically

2. Add Nightmare behavior:
- keep Normal/Hard at their current two-phase baseline unless explicit tuning demands otherwise
- define a third Nightmare-only attack phase with distinct timing or projectile pattern, not just recolored phase 2
- ensure boss HUD/currentPhase display still reports the correct total phase count if that UI is currently hardcoded to `2`

Do not special-case Nightmare boss behavior inside unrelated state code. The boss config/system pair must own the extra phase cleanly.
  </action>
  <verify>
    <automated>npx vitest run src/systems/BossSystem.test.ts src/states/PlayingState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Boss config and BossSystem support generic multi-phase progression, and Nightmare adds an actual third boss phase without regressing the existing two-phase encounter.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/state/MetaState.test.ts src/states/TitleState.test.ts src/states/PlayingState.test.ts src/ui/MetaShopUI.test.ts src/config/difficultyRules.test.ts src/config/waveConfig.test.ts src/systems/BossSystem.test.ts` passes
2. `npx tsc --noEmit` passes
3. Hard is hidden or disabled before purchase, then becomes selectable from the run-start path after purchase
4. Hard waves feel clearly harder through speed, density, and earlier Snipers while Normal plays unchanged
5. Nightmare cannot be bought before a Hard clear, becomes purchasable after that clear, and launches with more aggressive AI behavior plus a distinct extra boss phase
</verification>

<success_criteria>
- SHOP-04 is satisfied through a purchasable Hard entitlement surfaced at run start
- SHOP-05 is satisfied through centralized Hard rules covering wave speed, wave size, and earlier Snipers
- SHOP-06 is satisfied through persisted Hard-clear gating before Nightmare purchase/selection
- SHOP-07 is satisfied through difficulty-aware aggression tuning and a generalized boss system that supports one additional Nightmare phase
- The risky boss refactor remains isolated to combat/config code and does not get entangled with shop layout work
</success_criteria>

<output>
After completion, create `.planning/phases/10-meta-shop-expansion/10-02-SUMMARY.md`
</output>
