---
phase: 10-meta-shop-expansion
plan: 03
type: execute
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - src/ui/MetaShopUI.ts
  - src/config/metaUpgrades.ts
  - src/state/MetaState.ts
  - src/states/TitleState.ts
autonomous: true
requirements: [SHOP-01, SHOP-02, SHOP-04]

must_haves:
  truths:
    - "The expanded meta shop uses category tabs as the primary navigation model: Weapons, Ships, Upgrades, Visual"
    - "Relevant Phase 10 unlocks are easy to locate quickly because cards are grouped by category instead of one long scroll path"
    - "Locked upgrades stay hidden by default and can be revealed on demand without cluttering the default browse path"
    - "Keyboard and gamepad navigation remain intact across tab changes, reveal toggles, and purchase actions"
  artifacts:
    - path: "src/ui/MetaShopUI.ts"
      provides: "Tabbed meta-shop layout, grouped card rendering, locked-item reveal affordance, and navigation updates"
      exports: ["MetaShopUI"]
    - path: "src/config/metaUpgrades.ts"
      provides: "Stable category metadata consumed by the tabbed UI"
      exports: ["META_UPGRADES"]
    - path: "src/state/MetaState.ts"
      provides: "Any minimal persisted UI preference needed for the shop view, only if justified"
      exports: ["useMetaStore"]
  key_links:
    - from: "src/ui/MetaShopUI.ts"
      to: "src/config/metaUpgrades.ts"
      via: "groups upgrades into tab categories and derives locked visibility"
      pattern: "category|effectType|META_UPGRADES"
    - from: "src/ui/MetaShopUI.ts"
      to: "src/state/MetaState.ts"
      via: "reads owned upgrades, active toggles, and optional reveal preference while preserving purchase behavior"
      pattern: "purchasedUpgrades|toggle|difficulty|startingPowerUp"
---

<objective>
Reorganize the meta shop presentation into a faster-scanning category UI after the entitlement and difficulty foundations are stable.

Purpose: satisfy the Phase 10 usability goal without mixing layout work into the higher-risk combat and run-start changes.
Output: a tabbed MetaShopUI with category-based grouping, compact upgrade cards, and a reveal-on-demand path for locked content that preserves current keyboard/gamepad purchase flow.
</objective>

<execution_context>
@C:/Users/james/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-meta-shop-expansion/10-CONTEXT.md
@.planning/phases/10-meta-shop-expansion/10-RESEARCH.md
@.planning/phases/10-meta-shop-expansion/10-VALIDATION.md
@.planning/phases/10-meta-shop-expansion/10-01-PLAN.md
@.planning/phases/10-meta-shop-expansion/10-02-PLAN.md

<interfaces>
Current UI baseline from source inspection:

- `src/ui/MetaShopUI.ts` already renders a long vertical overlay with grouped sections, sequential-card helpers, flat `purchasableIds`, and keyboard/gamepad cursor support.
- The UI already contains ship-skin and CRT sections, so Phase 10 should reorganize existing and new content into tabs rather than reimplement shop purchasing from scratch.
- Research direction is explicit: category tabs are primary navigation, category names must be `Weapons`, `Ships`, `Upgrades`, and `Visual`, and locked items should be hidden by default but revealable on demand.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Convert MetaShopUI from long-scroll sections to category tabs</name>
  <files>src/ui/MetaShopUI.ts, src/config/metaUpgrades.ts</files>
  <action>
Refactor `src/ui/MetaShopUI.ts` so the top-level browse model is tabbed categories:
- `Weapons`
- `Ships`
- `Upgrades`
- `Visual`

Use the existing `META_UPGRADES` metadata as the grouping source rather than hardcoding card membership inside the render method. If the current category labels do not map cleanly, extend `src/config/metaUpgrades.ts` with the minimum metadata needed to support stable grouping.

Preserve or improve:
- purchase handling and sequential prerequisite locks
- existing special controls like bunker toggles and CRT controls
- the flat `purchasableIds` / selected-index navigation model, rebuilt from the active tab so gamepad support stays linear and predictable
  </action>
  <verify>
    <automated>npx vitest run src/ui/MetaShopUI.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>MetaShopUI is tab-driven, groups upgrades by category metadata, and keeps keyboard/gamepad purchase navigation working within the active tab.</done>
</task>

<task type="auto">
  <name>Task 2: Add reveal-on-demand locked browsing and compact card presentation</name>
  <files>src/ui/MetaShopUI.ts, src/state/MetaState.ts, src/states/TitleState.ts</files>
  <action>
Finish the UI slice by making the expanded catalog fast to scan:
- hide locked upgrades by default within each tab
- add a clear reveal affordance so players can intentionally inspect locked content
- keep upgrade cards compact: short effect text, cost, and ownership/lock state only
- ensure the new Phase 10 items are easy to find in sensible tabs:
  - starting slot in `Weapons`
  - extra lives and difficulty unlocks in `Upgrades`
  - ship skins in `Ships`
  - CRT in `Visual`

If a small persisted UI preference for active tab or locked reveal improves usability, add it only if justified; otherwise keep the view state local to MetaShopUI.

Update any input-hint text or entry-point messaging touched by the tabbed flow so gamepad and keyboard users still understand how to navigate.
  </action>
  <verify>
    <automated>npx vitest run src/ui/MetaShopUI.test.ts src/states/TitleState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The shop defaults to a clean, uncluttered browse path with optional locked reveal, and the navigation hints remain accurate for keyboard and gamepad users.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/ui/MetaShopUI.test.ts src/states/TitleState.test.ts` passes
2. `npx tsc --noEmit` passes
3. Opening the meta shop shows tabs for Weapons, Ships, Upgrades, and Visual instead of a single long-scroll catalog
4. Starting slot, extra lives, and Hard unlock can each be found in under ten seconds from the default shop view
5. Locked content is hidden by default but can be revealed intentionally without breaking selection or purchase behavior
</verification>

<success_criteria>
- The Phase 10 shop remains a presentation layer over the entitlement logic added in 10-01 and 10-02
- SHOP-01, SHOP-02, and SHOP-04 are easier to discover through category-based organization
- The default browse path stays uncluttered while preserving discoverability of future/locked content
- Keyboard and gamepad navigation stay intact after the UI reorganization
</success_criteria>

<output>
After completion, create `.planning/phases/10-meta-shop-expansion/10-03-SUMMARY.md`
</output>
