---
phase: 10-meta-shop-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/metaUpgrades.ts
  - src/state/MetaState.ts
  - src/states/TitleState.ts
  - src/states/PlayingState.ts
  - src/config/powerups.ts
autonomous: true
requirements: [SHOP-01, SHOP-02, SHOP-03]

must_haves:
  truths:
    - "Player can buy up to two permanent starting-life upgrades and fresh runs start with the extra lives applied once"
    - "Player can buy a starting power-up slot and sees a pre-run setup overlay before every Campaign or Endless launch"
    - "The pre-run setup overlay always includes an explicit NONE option and remembers the last selected difficulty and starting power-up"
    - "Starting power-up choices are derived from the currently unlocked meta loadout pool instead of a second hand-maintained list"
  artifacts:
    - path: "src/config/metaUpgrades.ts"
      provides: "Meta upgrade definitions for life tiers and starting-slot entitlement"
      exports: ["META_UPGRADES"]
    - path: "src/state/MetaState.ts"
      provides: "Persistent difficulty and starting-power-up setters plus authoritative helpers for run-setup preferences"
      exports: ["useMetaStore", "_migrate"]
    - path: "src/states/TitleState.ts"
      provides: "Run-setup overlay flow after Campaign/Endless selection and before PlayingState launch"
      exports: ["TitleState"]
    - path: "src/states/PlayingState.ts"
      provides: "Fresh-run-only application of extra lives and selected starting power-up"
      exports: ["PlayingState"]
  key_links:
    - from: "src/states/TitleState.ts"
      to: "src/state/MetaState.ts"
      via: "persists chosen difficulty and starting power-up before run launch"
      pattern: "setDifficulty|setStartingPowerUp|startingPowerUp|difficulty"
    - from: "src/states/PlayingState.ts"
      to: "src/state/MetaState.ts"
      via: "consumes stored run-start selections and owned entitlements at fresh run entry"
      pattern: "startingPowerUp|extraLivesPurchased|purchasedUpgrades"
    - from: "src/config/metaUpgrades.ts"
      to: "src/states/TitleState.ts"
      via: "defines which owned upgrades unlock run-setup options"
      pattern: "starting_powerup_slot|passive_startingLife"
---

<objective>
Implement the entitlement and run-setup foundation for Meta Shop Expansion: extra starting lives, starting power-up slot unlock, and a pre-run chooser that sits in the title flow rather than in the shop or PlayingState.

Purpose: deliver the player-facing loop for SHOP-01 through SHOP-03 with one authoritative persistence model and without coupling the chooser to the later shop-layout rewrite.
Output: new meta upgrade definitions, MetaState actions/helpers for run-start preferences, a title-screen run-setup overlay, and PlayingState logic that applies the selected bonuses only on fresh launches.
</objective>

<execution_context>
@C:/Users/james/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-meta-shop-expansion/10-CONTEXT.md
@.planning/phases/10-meta-shop-expansion/10-RESEARCH.md
@.planning/phases/10-meta-shop-expansion/10-VALIDATION.md

<interfaces>
Current seams confirmed by research and code inspection:

- `src/state/MetaState.ts` already persists `difficulty`, `startingPowerUp`, and `extraLivesPurchased`, but lacks dedicated setters for the run-setup fields and currently types `startingPowerUp` as a string upgrade id.
- `src/states/TitleState.ts` already owns Campaign/Endless launch and optional campaign level selection; Phase 10 should add the run-setup overlay here after the mode path resolves.
- `src/states/PlayingState.ts` currently applies `loadout_spread_start` and `loadout_rapid_start` directly inside `applyMetaBonuses()`. That hardcoded path must be replaced with selection-based activation while preserving existing passive bonus behavior.
- `src/config/metaUpgrades.ts` remains the authoritative catalog for permanent entitlements.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend meta entitlements and state actions for run-start setup</name>
  <files>src/config/metaUpgrades.ts, src/state/MetaState.ts, src/config/powerups.ts</files>
  <action>
Update `src/config/metaUpgrades.ts` to add the new permanent upgrade entries needed for this slice:
- two sequential starting-life upgrades using ids that clearly encode tier 1 and tier 2
- one starting power-up slot unlock
- keep the category/effectType naming aligned with the existing meta-shop conventions so later tabbed UI grouping can sort these into `Weapons` and `Upgrades`

In `src/state/MetaState.ts`:
- add explicit `setDifficulty()` and `setStartingPowerUp()` actions to the store interface and implementation
- tighten the `startingPowerUp` type from a loose upgrade-id string to `PowerUpType | null`
- decide a single authority for extra lives; follow research guidance and use owned upgrade ids as truth, with `extraLivesPurchased` either derived in one helper or updated in exactly one place to avoid drift
- keep migration versioning unchanged unless the chosen type tightening truly requires a schema correction

Add one small helper surface, either in `MetaState.ts` or a nearby helper module, that returns the unlocked run-start power-up options from currently owned meta upgrades. It must include the existing loadout unlocks and the Phase 9 power-ups that become eligible in v1.1.
  </action>
  <verify>
    <automated>npx vitest run src/state/MetaState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Meta entitlements exist for +2 lives and the starting slot, MetaState exposes dedicated setters for difficulty and starting power-up, and unlocked starting choices can be derived from owned upgrades without duplicating truth.</done>
</task>

<task type="auto">
  <name>Task 2: Add a title-screen run-setup overlay before fresh launches</name>
  <files>src/states/TitleState.ts, src/state/MetaState.ts</files>
  <action>
Modify `src/states/TitleState.ts` so the launch flow becomes:
1. player chooses `Campaign` or `Endless`
2. if campaign level selection is needed, resolve that first
3. open a new run-setup overlay
4. let the player confirm difficulty from the unlocked set
5. if the starting-slot entitlement is owned, let the player choose a starting power-up or `NONE`
6. launch `PlayingState`

Implementation requirements:
- keep this as a DOM overlay following the existing TitleState/menu style rather than opening MetaShopUI
- read the last selected difficulty and starting power-up from MetaState so the flow remembers previous choices
- always include a visible `NONE` option even when the player owns the starting slot
- disable or hide `Hard` and `Nightmare` unless they are unlocked; Normal must always remain selectable
- support keyboard/gamepad navigation using the same synthesized input layer TitleState already uses
- do not move difficulty switching into the meta shop
  </action>
  <verify>
    <automated>npx vitest run src/states/TitleState.test.ts src/state/MetaState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>TitleState launches through a run-setup overlay that remembers prior choices, includes a NONE option, and only exposes entitled difficulty and loadout options.</done>
</task>

<task type="auto">
  <name>Task 3: Apply fresh-run bonuses cleanly in PlayingState</name>
  <files>src/states/PlayingState.ts, src/state/MetaState.ts</files>
  <action>
Refactor `src/states/PlayingState.ts` so `applyMetaBonuses()` consumes the run-start selections produced by Task 2:
- replace the hardcoded `loadout_spread_start` / `loadout_rapid_start` auto-activation with activation of the selected `startingPowerUp`
- apply extra lives from the owned tier count, capped at +2 total
- keep existing passive fire-rate, move-speed, bunker, skin, and CRT behavior intact
- explicitly guard the fresh-run boundary so continue/restart paths do not re-grant starting lives or the starting power-up unintentionally

If the cleanest implementation needs a small boolean or helper to distinguish fresh launch from re-entry, add it in the narrowest place possible and document the intended behavior in code comments near the guard.
  </action>
  <verify>
    <automated>npx vitest run src/states/PlayingState.test.ts src/state/MetaState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Fresh runs apply the selected starting power-up and extra lives exactly once, while non-fresh re-entry paths do not duplicate the bonuses.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/state/MetaState.test.ts src/states/TitleState.test.ts src/states/PlayingState.test.ts` passes
2. `npx tsc --noEmit` passes
3. Buying both life tiers and starting a fresh run yields base lives plus two additional lives, with no further gain on continue/restart
4. Buying the starting slot causes the run-setup overlay to appear before both Campaign and Endless launches
5. The run-setup overlay always offers `NONE`, remembers the last selected choices, and never offers a power-up that is still locked
</verification>

<success_criteria>
- SHOP-01 is satisfied through permanent +2 starting-life ownership applied at fresh run start only
- SHOP-02 is satisfied through a purchasable starting-slot entitlement surfaced in the run-start flow
- SHOP-03 is satisfied through a pre-run chooser that persists the last selection and includes an explicit `NONE` option
- TitleState remains the owner of setup flow and PlayingState remains the consumer of finalized setup state
</success_criteria>

<output>
After completion, create `.planning/phases/10-meta-shop-expansion/10-01-SUMMARY.md`
</output>
