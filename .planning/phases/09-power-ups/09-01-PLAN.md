---
phase: 09-power-ups
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/config/powerups.ts
  - src/entities/Bullet.ts
  - src/systems/WeaponSystem.ts
  - src/systems/CollisionSystem.ts
  - src/effects/ParticleManager.ts
autonomous: true
requirements: [PWR-01, PWR-02]

must_haves:
  truths:
    - "Piercing shot can damage two enemies in a line with one fired projectile"
    - "The first pierced enemy takes full damage and the second takes reduced damage"
    - "Piercing bullets are not consumed on the first enemy hit"
    - "Piercing shots read differently from standard bullets through an elongated rail-style trail"
  artifacts:
    - path: "src/config/powerups.ts"
      provides: "Piercing power-up definition and typed metadata for the expanded power-up set"
      exports: ["POWER_UP_DEFS", "POWER_UP_TYPES"]
    - path: "src/entities/Bullet.ts"
      provides: "Projectile metadata for piercing behavior and visual identity"
      exports: ["Bullet"]
    - path: "src/systems/WeaponSystem.ts"
      provides: "Player fire path that spawns piercing-configured bullets when the power-up is active"
      exports: ["WeaponSystem"]
    - path: "src/systems/CollisionSystem.ts"
      provides: "Multi-hit collision handling for piercing bullets"
      exports: ["CollisionSystem"]
    - path: "src/effects/ParticleManager.ts"
      provides: "Piercing-specific trail or impact feedback helpers"
      exports: ["ParticleManager"]
  key_links:
    - from: "src/systems/WeaponSystem.ts"
      to: "src/entities/Bullet.ts"
      via: "configures piercing bullet metadata after acquire/init"
      pattern: "piercing|remainingHits|damage"
    - from: "src/systems/CollisionSystem.ts"
      to: "src/entities/Bullet.ts"
      via: "reads piercing metadata to decide whether the bullet survives the hit"
      pattern: "remainingHits|shotKind|damage"
    - from: "src/systems/WeaponSystem.ts"
      to: "src/effects/ParticleManager.ts"
      via: "triggers distinct piercing trail feedback"
      pattern: "piercing"
---

<objective>
Implement the core piercing-shot behavior on the existing player bullet path, including multi-hit collision handling and a distinctly readable rail-style projectile/trail treatment.

Purpose: Deliver the first new power-up with minimal new infrastructure by extending the current bullet system instead of adding a second generic projectile abstraction.
Output: expanded power-up definitions, bullet metadata for piercing, collision logic that allows two hits in one line, and a visible elongated trail/impact read for the piercing shot.
</objective>

<execution_context>
@C:/Users/james/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-power-ups/09-CONTEXT.md
@.planning/phases/09-power-ups/09-RESEARCH.md
@.planning/phases/09-power-ups/09-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expand the power-up schema and bullet metadata for piercing shots</name>
  <files>src/config/powerups.ts, src/entities/Bullet.ts, src/systems/WeaponSystem.ts</files>
  <action>
Update `src/config/powerups.ts` to add `piercingShot`, `homingMissile`, and `timeSlow` to `PowerUpType`, and define names, durations, and colors for all three.

Update `src/entities/Bullet.ts` so pooled player bullets can carry explicit weapon metadata:
- shot kind such as `'standard' | 'spread' | 'piercing'`
- remaining pierces / remaining hits
- damage or damage-scale metadata needed for first-hit full damage plus second-hit reduced damage
- reset all new fields in `init()` so pooled bullets never leak old state

Update `src/systems/WeaponSystem.ts`:
- branch on `powerUpManager.isActive('piercingShot')`
- fire one straight piercing projectile instead of a spread pattern when active
- assign the piercing metadata on the acquired bullet
- trigger a distinct rail-style visual read rather than relying on a color swap alone
  </action>
  <verify>
    <automated>npx vitest run src/entities/Bullet.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>`powerups.ts` knows about `piercingShot`, `Bullet` safely resets/configures piercing metadata, and `WeaponSystem` emits a visually distinct piercing projectile.</done>
</task>

<task type="auto">
  <name>Task 2: Add multi-hit collision handling and piercing feedback</name>
  <files>src/systems/CollisionSystem.ts, src/effects/ParticleManager.ts</files>
  <action>
Modify `src/systems/CollisionSystem.ts` so piercing bullets are not always consumed on first enemy hit:
- branch on the bullet metadata added in Task 1
- apply full damage to the first enemy, then keep the bullet alive if a second hit is allowed
- apply reduced damage to the second enemy and then consume the projectile
- prevent the same enemy from being damaged twice by the same bullet on adjacent overlap frames
- preserve existing shield, score, gold-drop, and particle/audio behavior when enemies die

Extend `src/effects/ParticleManager.ts` with a small piercing-feedback helper for elongated trail / through-line impact read. Keep it compatible with the current pooled particle approach.
  </action>
  <verify>
    <automated>npx vitest run src/entities/Bullet.test.ts src/systems/CollisionSystem.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Collision resolution supports two-target piercing, the second hit is reduced, and the projectile/trail read is visibly distinct from the standard shot.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/entities/Bullet.test.ts src/systems/CollisionSystem.test.ts` passes
2. `npx tsc --noEmit` passes
3. In a live run, activating piercing shot lets one projectile damage two enemies lined up vertically
4. The first enemy takes full damage, the second takes reduced damage, and the bullet does not outlive its configured penetration count
5. The fired shot has an elongated rail-style trail that is clearly distinguishable from standard and spread shots
</verification>

<success_criteria>
- PWR-01 is satisfied through actual multi-hit line penetration
- PWR-02 is satisfied through a distinct projectile/trail read, not just recolor
- Bullet pooling remains safe with explicit metadata reset on init
- Existing standard / spread / rapid-fire behavior is not regressed
</success_criteria>

<output>
After completion, create `.planning/phases/09-power-ups/09-01-SUMMARY.md`
</output>
