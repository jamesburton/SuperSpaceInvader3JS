---
phase: 09-power-ups
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/entities/HomingMissile.ts
  - src/systems/HomingMissileManager.ts
  - src/systems/WeaponSystem.ts
  - src/states/PlayingState.ts
  - src/entities/Enemy.ts
autonomous: true
requirements: [PWR-03, PWR-04]

must_haves:
  truths:
    - "Homing missiles steer toward a locked enemy with a capped turn rate instead of instant snapping"
    - "Each missile expires after a finite lifetime even if it never hits"
    - "A target lock reticle is visible on the missile's current target"
    - "Missiles handle target death gracefully without crashing or stalling the run loop"
  artifacts:
    - path: "src/entities/HomingMissile.ts"
      provides: "Dedicated pooled missile entity with heading, lifetime, and target metadata"
      exports: ["HomingMissile"]
    - path: "src/systems/HomingMissileManager.ts"
      provides: "Spawn/update/release loop for missiles and lock reticles"
      exports: ["HomingMissileManager"]
    - path: "src/systems/WeaponSystem.ts"
      provides: "Delegation path that fires missiles when the homing power-up is active"
      exports: ["WeaponSystem"]
    - path: "src/states/PlayingState.ts"
      provides: "Missile manager update orchestration inside the gameplay loop"
      exports: ["PlayingState"]
  key_links:
    - from: "src/systems/WeaponSystem.ts"
      to: "src/systems/HomingMissileManager.ts"
      via: "spawns a missile instead of using the shared bullet pool"
      pattern: "spawnMissile|homing"
    - from: "src/systems/HomingMissileManager.ts"
      to: "src/entities/Enemy.ts"
      via: "reads active target position / identity for steering and reticle placement"
      pattern: "instanceIndex|active"
    - from: "src/states/PlayingState.ts"
      to: "src/systems/HomingMissileManager.ts"
      via: "updates missiles each fixed step and includes them in cleanup/reset paths"
      pattern: "HomingMissileManager|update|releaseAll"
---

<objective>
Add the dedicated homing-missile projectile path, including limited-turn steering, lifetime expiry, and a subtle lock-on reticle that tracks the targeted enemy.

Purpose: Deliver the second power-up without overloading the generic bullet system, following the locked project decision that homing should use its own dedicated mesh path.
Output: pooled homing missile entity + manager, WeaponSystem integration, gameplay-loop updates, and a visible target-reticle read.
</objective>

<execution_context>
@C:/Users/james/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-power-ups/09-CONTEXT.md
@.planning/phases/09-power-ups/09-RESEARCH.md
@.planning/phases/09-power-ups/09-VALIDATION.md
@.planning/phases/09-power-ups/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the dedicated homing missile entity and manager</name>
  <files>src/entities/HomingMissile.ts, src/systems/HomingMissileManager.ts, src/entities/Enemy.ts</files>
  <action>
Create a dedicated missile path instead of extending `Bullet` again:
- add `src/entities/HomingMissile.ts` as a pooled entity with active state, position, heading, speed, lifetime remaining, and target identity/reference
- give the missile a directional mesh or orientation-capable representation that can rotate toward its steering heading
- add `src/systems/HomingMissileManager.ts` to own the missile pool plus any reticle pool / visual markers
- implement target acquisition as "nearest active enemy at fire time", matching `09-CONTEXT.md`
- enforce conservative steering with a capped turn rate and a fixed lifetime cap so missiles can miss
- when the current target dies, either expire or allow a single conservative reacquire, but do not drift into fully sticky behavior
- if `Enemy` data needs a stable identifier beyond `instanceIndex`, add only the minimal metadata needed for safe tracking
  </action>
  <verify>
    <automated>npx vitest run src/systems/HomingMissileManager.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>A dedicated homing missile manager exists with conservative steering, finite lifetime, and safe target-loss handling.</done>
</task>

<task type="auto">
  <name>Task 2: Wire homing fire, updates, and the lock-on reticle into gameplay</name>
  <files>src/systems/WeaponSystem.ts, src/states/PlayingState.ts</files>
  <action>
Integrate the new manager into the gameplay loop:
- update `WeaponSystem` so an active `homingMissile` power-up spawns a missile via `HomingMissileManager` instead of consuming the shared player bullet pool
- keep the fire cadence aligned with context: one missile per shot, not paired missiles
- add the manager to `PlayingState` orchestration so missiles update each fixed step, clear on continue/reset, and coexist with boss/shop/game-over transitions
- ensure missile hits contribute score / enemy-kill side effects consistently with the existing combat path
- implement a subtle neon reticle on the targeted enemy that tracks the target while valid and clears cleanly on hit, expiry, or target loss
  </action>
  <verify>
    <automated>npx vitest run src/systems/HomingMissileManager.test.ts src/states/PlayingState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Activating homing missiles replaces the normal shot with a single imperfect-seeking missile and a readable lock-on reticle.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/systems/HomingMissileManager.test.ts src/states/PlayingState.test.ts` passes
2. `npx tsc --noEmit` passes
3. In a live run, firing with the homing power-up locks the nearest enemy at fire time
4. The missile curves toward its target with a visible but limited turn rate and can still miss or expire
5. A subtle reticle appears on the target, tracks it while valid, and clears correctly on hit, expiry, or target death
</verification>

<success_criteria>
- PWR-03 is satisfied with limited-turn, finite-lifetime missiles
- PWR-04 is satisfied with a target reticle that is present and readable but not oversized
- The shared bullet pool remains simple; homing uses its own dedicated path
- No stale-target crashes or orphaned reticles remain after target death or run reset
</success_criteria>

<output>
After completion, create `.planning/phases/09-power-ups/09-02-SUMMARY.md`
</output>
