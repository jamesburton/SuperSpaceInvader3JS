---
phase: 09-power-ups
plan: 03
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - src/state/RunState.ts
  - src/systems/PowerUpManager.ts
  - src/states/PlayingState.ts
  - src/ui/HUD.ts
  - src/systems/MovementSystem.ts
autonomous: true
requirements: [PWR-05, PWR-06]

must_haves:
  truths:
    - "Time slow reduces enemy and projectile speed without slowing the player's movement or fire input"
    - "The slowdown is time-bounded and resets cleanly when the power-up expires or the run resets"
    - "A cool-tinted desaturation treatment appears while time slow is active"
    - "The activation and release feel eased rather than abrupt"
  artifacts:
    - path: "src/state/RunState.ts"
      provides: "Readable combat time-scale state if shared runtime storage is required"
      exports: ["runState"]
    - path: "src/systems/PowerUpManager.ts"
      provides: "Time-slow active-state exposure and duration ownership"
      exports: ["PowerUpManager"]
    - path: "src/states/PlayingState.ts"
      provides: "Selective delta-time routing and visual-treatment orchestration"
      exports: ["PlayingState"]
    - path: "src/ui/HUD.ts"
      provides: "Optional overlay/tint host elements for time-slow presentation"
      exports: ["HUD"]
  key_links:
    - from: "src/states/PlayingState.ts"
      to: "src/systems/PowerUpManager.ts"
      via: "reads time-slow active state and derives scaled enemy dt"
      pattern: "timeSlow|timeScale"
    - from: "src/states/PlayingState.ts"
      to: "src/ui/HUD.ts"
      via: "applies or updates the slowdown visual treatment"
      pattern: "tint|overlay|desatur"
    - from: "src/states/PlayingState.ts"
      to: "src/systems/MovementSystem.ts"
      via: "routes scaled dt for hostile/projectile movement only"
      pattern: "updateBullets"
---

<objective>
Implement the time-slow power-up by selectively slowing enemies and hostile/projectile motion while leaving the player at full speed, and pair it with a readable cool-tinted desaturation treatment.

Purpose: Deliver the third new power-up as a combat-state modifier rather than a global game slow-down, matching the locked project decision in `STATE.md`.
Output: time-slow state exposure, selective delta-time routing in gameplay, and an eased visual treatment that communicates the active slowdown.
</objective>

<execution_context>
@C:/Users/james/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-power-ups/09-CONTEXT.md
@.planning/phases/09-power-ups/09-RESEARCH.md
@.planning/phases/09-power-ups/09-VALIDATION.md
@.planning/phases/09-power-ups/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose time-slow state and route scaled delta time through combat systems</name>
  <files>src/state/RunState.ts, src/systems/PowerUpManager.ts, src/states/PlayingState.ts, src/systems/MovementSystem.ts</files>
  <action>
Add the runtime state needed for selective slow motion:
- expand the power-up flow so `timeSlow` can be activated as a timed power-up through `PowerUpManager`
- expose a readable slowdown factor, either through `PowerUpManager` directly or via a small `RunState` field if that makes downstream access simpler
- in `PlayingState.update()`, keep `player.update()` and `weaponSystem.update()` on raw `dt`
- route slowed `dt` into enemy AI, boss logic, and hostile/projectile movement
- ensure shield logic, shop pausing, continue/reset, and end-of-run cleanup all reset the slowdown state cleanly
- if `MovementSystem.updateBullets()` currently applies the same delta to player and enemy bullets, split it so player bullets can remain full-speed while enemy bullets slow
  </action>
  <verify>
    <automated>npx vitest run src/state/RunState.test.ts src/states/PlayingState.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Gameplay routes enemy-facing systems through a reduced time scale while the player remains on full-speed controls and firing.</done>
</task>

<task type="auto">
  <name>Task 2: Add the time-slow visual treatment and easing</name>
  <files>src/states/PlayingState.ts, src/ui/HUD.ts</files>
  <action>
Add the visual communication layer for time slow:
- implement a cool-tinted desaturation treatment that can be toggled during gameplay without introducing a heavy new rendering dependency
- prefer a HUD-owned fullscreen overlay or scene-adjacent DOM layer if that is the smallest clean fit
- ease the activation and release with a short fade rather than a hard pop, matching `09-CONTEXT.md`
- keep bullets, enemies, and the player readable under current bloom/CRT conditions
- if needed, surface the active state through existing HUD helpers so the player reads both timer and visual state together
  </action>
  <verify>
    <automated>npx vitest run src/states/PlayingState.test.ts src/ui/HUD.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Time slow has an eased cool-tinted/desaturated presentation that clearly communicates the state without obscuring gameplay.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/state/RunState.test.ts src/states/PlayingState.test.ts src/ui/HUD.test.ts` passes
2. `npx tsc --noEmit` passes
3. In a live run, activating time slow visibly slows enemies and enemy bullets while the player ship still moves and fires at normal speed
4. The visual treatment fades in and out smoothly and remains readable under current bloom/CRT settings
5. Expiration, continue, and run reset all clear the slowdown state and remove the tint reliably
</verification>

<success_criteria>
- PWR-05 is satisfied via selective enemy/projectile slowdown
- PWR-06 is satisfied via visible cool tint/desaturation, not timer text alone
- Player controls remain crisp during the effect
- No residual time-scale state survives reset, continue, or game-over transitions
</success_criteria>

<output>
After completion, create `.planning/phases/09-power-ups/09-03-SUMMARY.md`
</output>
