---
phase: 09-power-ups
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - src/systems/ShopSystem.ts
  - src/systems/PowerUpManager.ts
  - src/entities/PickupToken.ts
  - src/config/powerups.ts
  - src/ui/ShopUI.ts
autonomous: true
requirements: [PWR-07, PWR-08]

must_haves:
  truths:
    - "Piercing shot, homing missile, and time slow can each be bought from the between-wave shop"
    - "Buying one of the new shop items immediately activates the matching timed power-up"
    - "The three pickup drops are distinguishable at a glance by shape and presentation, not color alone"
    - "The shop remains readable after adding the new item set"
  artifacts:
    - path: "src/systems/ShopSystem.ts"
      provides: "Shop item registration and purchase behavior for the three new power-ups"
      exports: ["ShopSystem", "SHOP_ITEMS"]
    - path: "src/systems/PowerUpManager.ts"
      provides: "Activation entry points used by shop purchases"
      exports: ["PowerUpManager"]
    - path: "src/entities/PickupToken.ts"
      provides: "Per-type pickup silhouettes / variant visuals"
      exports: ["PickupToken"]
    - path: "src/ui/ShopUI.ts"
      provides: "Shop rendering that accommodates the expanded power-up item list"
      exports: ["ShopUI"]
  key_links:
    - from: "src/systems/ShopSystem.ts"
      to: "src/systems/PowerUpManager.ts"
      via: "activates a chosen timed power-up immediately on purchase"
      pattern: "activate|powerUp"
    - from: "src/entities/PickupToken.ts"
      to: "src/config/powerups.ts"
      via: "reads power-up definition metadata and per-type appearance hints"
      pattern: "POWER_UP_DEFS|color"
    - from: "src/ui/ShopUI.ts"
      to: "src/systems/ShopSystem.ts"
      via: "renders the expanded list of shop items without breaking interaction flow"
      pattern: "getAvailableItems|displayName|description"
---

<objective>
Expose the three new power-ups in the between-wave shop and give each drop token a distinct pickup appearance so players can recognize them immediately.

Purpose: Finish the phase by making the new mechanics discoverable through both acquisition paths: random pickups during combat and explicit between-wave shop purchases.
Output: new shop items wired to activate the power-ups, distinct pickup token visuals for all three, and a shop UI that still reads cleanly with the larger catalog.
</objective>

<execution_context>
@C:/Users/james/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-power-ups/09-CONTEXT.md
@.planning/phases/09-power-ups/09-RESEARCH.md
@.planning/phases/09-power-ups/09-VALIDATION.md
@.planning/phases/09-power-ups/09-02-SUMMARY.md
@.planning/phases/09-power-ups/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the new power-ups as between-wave shop purchases</name>
  <files>src/systems/ShopSystem.ts, src/systems/PowerUpManager.ts, src/config/powerups.ts</files>
  <action>
Extend the between-wave shop so all three Phase 9 power-ups are purchasable:
- add three new shop items for piercing shot, homing missile, and time slow with prices/descriptions that position them as sidegrades beside spread shot, rapid fire, and shield
- update `ShopSystem` categories if needed so the new entries fit cleanly without overloading unrelated categories
- wire purchase handling so buying one of these items immediately activates the corresponding timed power-up via `PowerUpManager.activate()`
- preserve the current "one timed power-up active at once" model unless an earlier plan explicitly changed it
- if activation callbacks are needed to avoid new circular imports, follow the existing callback-injection pattern used for shield / bunker purchases
  </action>
  <verify>
    <automated>npx vitest run src/systems/ShopSystem.test.ts src/systems/PowerUpManager.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>The shop offers all three new power-ups and purchasing one activates the matching timed effect immediately.</done>
</task>

<task type="auto">
  <name>Task 2: Give each new pickup token a distinct appearance and keep the shop readable</name>
  <files>src/entities/PickupToken.ts, src/ui/ShopUI.ts</files>
  <action>
Update `src/entities/PickupToken.ts` so the three new power-ups are visually distinct at a glance:
- keep the pooled ownership model intact, but vary shape, silhouette, or secondary accents by type instead of relying on emissive color alone
- align the token identities with context:
  - piercing shot: elongated rail-like or shard-like token
  - homing missile: directional / arrow-like token
  - time slow: cool-tinted geometric token that implies temporal distortion
- ensure any geometry/material changes are reset correctly when pooled tokens are reused

Update `src/ui/ShopUI.ts` only as needed to keep the expanded item list readable:
- confirm the added entries fit in the existing scrollable list
- preserve keyboard/gamepad navigation
- improve category labeling or description density only if needed; avoid a full redesign
  </action>
  <verify>
    <automated>npx vitest run src/entities/PickupToken.test.ts src/systems/ShopSystem.test.ts && npx tsc --noEmit</automated>
  </verify>
  <done>Pickup drops for all three new power-ups are distinguishable at a glance, and the shop still presents the expanded item list clearly.</done>
</task>

</tasks>

<verification>
1. `npx vitest run src/systems/ShopSystem.test.ts src/systems/PowerUpManager.test.ts src/entities/PickupToken.test.ts` passes
2. `npx tsc --noEmit` passes
3. In a live run, the between-wave shop lists piercing shot, homing missile, and time slow as purchasable options
4. Buying each one immediately activates the corresponding timed power-up and updates the existing HUD timer path
5. Dropped pickups for the three new power-ups are visually distinct even under bloom/CRT and can be identified quickly in motion
</verification>

<success_criteria>
- PWR-07 is satisfied through actual between-wave shop availability for all three power-ups
- PWR-08 is satisfied through shape/presentation differentiation, not just hue differences
- The current shop input/navigation model remains intact
- Timed power-up replacement behavior stays predictable and consistent with the existing design
</success_criteria>

<output>
After completion, create `.planning/phases/09-power-ups/09-04-SUMMARY.md`
</output>
