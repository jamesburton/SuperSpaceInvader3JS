---
phase: 05-campaign-mode-game-modes
plan: "06"
type: execute
wave: 4
depends_on:
  - "05-05"
  - "05-04"
  - "05-03"
  - "05-02"
  - "05-01"
files_modified: []
autonomous: false
requirements:
  - MODE-01
  - MODE-02
  - MODE-03
  - MODE-04
  - CAMP-01
  - CAMP-02
  - CAMP-03

must_haves:
  truths:
    - "Main menu shows Campaign and Endless as clearly labelled choices with descriptions"
    - "Endless mode launches an infinite escalating wave run (existing behavior, renamed)"
    - "Campaign mode launches Chapter 1: 4 levels with atmospheric briefings before each level"
    - "Defeating the boss in Campaign shows VICTORY screen; progress is saved"
    - "Returning to menu after a run and starting again works correctly (no game-over-screen bug)"
    - "Refreshing the browser preserves campaign progress (localStorage persistence verified)"
    - "Level select appears when player has completed at least one campaign level"
    - "Locked levels in level select are visually greyed out, not hidden"
    - "Briefings can be skipped immediately by pressing any key"
    - "All 7 Phase 5 requirements are met"
  artifacts:
    - path: "src/config/campaign.ts"
      provides: "CAMP-01: data-driven wave scripts"
    - path: "src/states/LevelBriefingState.ts"
      provides: "CAMP-03: atmospheric briefing overlay"
    - path: "src/states/TitleState.ts"
      provides: "MODE-03: mode selection UI, MODE-04: level select resume"
    - path: "src/state/MetaState.ts"
      provides: "MODE-04: campaign progress localStorage persistence"
  key_links:
    - from: "TitleState"
      to: "PlayingState"
      via: "runState.mode set before replace()"
      pattern: "verify by launching both modes"
    - from: "PlayingState"
      to: "LevelBriefingState"
      via: "stateManager.push() on level complete"
      pattern: "verify by completing level 1"
---

<objective>
Human verification checkpoint: confirm all 7 Phase 5 requirements are working as intended across both game modes.

Purpose: The executor has automated all implementation. This checkpoint confirms the complete Phase 5 experience — mode selection, campaign loop, briefings, progress persistence — before the phase is marked complete.
Output: Human approval or list of issues to fix.
</objective>

<execution_context>
@C:/Users/james/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/james/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 5 implementation:
    - Mode selection UI in TitleState (Campaign [C], Endless [E], Upgrades [U] with arrow-key nav)
    - Endless mode: existing infinite wave behavior, now explicitly selectable
    - Campaign Chapter 1: 4 handcrafted levels, data-driven wave configs, boss on Level 4
    - LevelBriefingState: atmospheric text overlay between campaign levels (skippable)
    - Campaign progress persistence: MetaStore SAVE_VERSION 3 with campaignProgress field
    - Level select resume: appears when player has unlocked start points beyond Level 1
    - Bug fix: returning to menu and restarting no longer lands on game-over screen
  </what-built>
  <how-to-verify>
    Run the dev server: `npm run dev` then open http://localhost:5173

    **1. Mode Select UI (MODE-03)**
    - [ ] Title screen shows three options: Campaign, Endless, Upgrades with descriptions
    - [ ] Arrow Down/Up cycles the highlighted selection
    - [ ] C/E keys select and immediately launch the respective mode
    - [ ] U key opens the Upgrades/MetaShop overlay
    - [ ] Campaign is highlighted by default on first load

    **2. Endless Mode (MODE-01)**
    - [ ] Press E (or select Endless + Enter) — game launches with infinite waves
    - [ ] Waves escalate as before; shop appears every 5th wave; boss appears at wave 10
    - [ ] Game over → return to menu → select Endless again — game starts fresh (bug fix)

    **3. Campaign Mode — Level Briefings (CAMP-03)**
    - [ ] Press C to start Campaign — Level 1 "Breach Point" briefing appears
    - [ ] Briefing shows chapter/level label, level title, atmospheric paragraph, "PRESS ANY KEY TO BEGIN"
    - [ ] Pressing any key dismisses briefing and gameplay begins
    - [ ] Complete Level 1 (3 waves) — Level 2 "Shield Wall" briefing appears

    **4. Campaign Mode — Level Flow (MODE-02, CAMP-01, CAMP-02)**
    - [ ] Level 1: 3 grunt-heavy waves (WAVE_CONFIGS[0..2])
    - [ ] Level 2: 4 waves with shielders (WAVE_CONFIGS[3..6])
    - [ ] Level 3: 3 waves with snipers (WAVE_CONFIGS[7..9])
    - [ ] Level 4: 2 waves then boss encounter (WAVE_CONFIGS[10..11] + boss)
    - [ ] Boss defeat triggers VICTORY screen

    **5. Campaign Progress Persistence (MODE-04)**
    - [ ] Complete Level 1; return to menu (press M on game over or ESC → M)
    - [ ] Select Campaign — level select appears showing Level 1 available, Level 2 unlocked, Levels 3-4 locked (greyed)
    - [ ] Refresh the browser (F5) — title screen shows; select Campaign — level select still appears (progress persisted in localStorage)
    - [ ] Select Level 2 — campaign starts from Level 2 with fresh run (no carry-in gold)
    - [ ] Open browser DevTools → Application → localStorage — confirm `ssi-meta-v1` key exists with `campaignProgress` field

    **6. Return-to-Menu Bug Fix**
    - [ ] Start any mode → die → press M to return to menu → press C to start Campaign
    - [ ] Game starts fresh — NOT stuck on game over screen; lives are at full

    **7. Locked Levels Visual (from CONTEXT.md specifics)**
    - [ ] In level select, locked levels are visually greyed out (opacity ~0.3), not hidden
    - [ ] Cannot click on locked levels

    Report any issues found or type "approved" to complete Phase 5.
  </how-to-verify>
  <resume-signal>Type "approved" to mark Phase 5 complete, or describe specific issues to fix.</resume-signal>
</task>

</tasks>

<verification>
All 7 Phase 5 requirements verified by human:
- MODE-01: Endless mode always available, infinite escalation
- MODE-02: Campaign Chapter 1 with 3-4 levels then boss
- MODE-03: Mode selection clearly presented on main menu
- MODE-04: Campaign progress saved to localStorage, resumable
- CAMP-01: Wave scripts are data-driven TypeScript objects
- CAMP-02: Chapter 1 has 4 handcrafted levels before boss
- CAMP-03: Atmospheric briefing text overlay between levels
</verification>

<success_criteria>
Human confirms all checklist items pass. Phase 5 is complete.
</success_criteria>

<output>
After completion, create `.planning/phases/05-campaign-mode-game-modes/05-06-SUMMARY.md`
</output>
